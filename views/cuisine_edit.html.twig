<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Edit a Cuisine</title>
    </head>
    <body>
        <div class="container">
            <h1>Update the {{cuisine.getType}} cuisine.</h1>

            <form action="/cuisines/{{cuisine.getId}}" method="post">
                <input name="_method" type="hidden" value="patch">

                <label for="new_type">Rename your cuisine:</label>
                <input id="new_type" name="new_type" type="text">

                <button type="submit">Update</button>
            </form>
        </div>
    </body>
</html>
